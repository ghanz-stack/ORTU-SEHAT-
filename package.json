{
  "name": "ortu-sehat",
  "version": "1.0.0",
  "description": "Ortu Sehat - Sedentary & Intermittent Fasting Tracker",
  "private": true,
  "scripts": {
    "assets": "capacitor-assets generate --android",
    "sync": "cap sync android",
    "build": "npm run assets && npm run sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/filesystem": "^6.0.3",
    "@capacitor/share": "^6.0.3"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
